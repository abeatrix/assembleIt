<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../partials/head.ejs', {title: "ALL POSTS"}) %>
  </head>
  <body>
    <main>
      <ul>
        <% posts.forEach(post => { %>
        <li>
          <a href="/<%= post._id %>"> <%= post.title %> </a>
        </li>
        <% }) %>
      </ul>
    <%- include('../partials/head.ejs', {title: "ALL POSTS"}) %>
<body>

      <% if(posts.length === 0) { %>
      <p>There are no posts available yet.</p>
      <% } %>
    </main>
  </body>
</html>
